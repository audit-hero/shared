{
  "name": "ah-shared",
  "version": "0.0.0",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "ts:defs": "tsc --declaration --outDir . --emitDeclarationOnly",
    "build": "tsc --build",
    "prepack": "yarn build && yarn ts:defs"
  },
  "devDependencies": {
    "@types/node": "^20.1.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
